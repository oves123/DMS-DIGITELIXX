services:
  - type: web
    name: dms-api
    env: docker
    rootDir: apps/api
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
